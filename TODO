* Code
  - fix DPF randomness state (> 256)
  - Use fastxor everywhere for PIR
  - parallelize server in both VPIR and PIR
  - Simplify DPF server API

* Experiments
  - Check constants in toml files
