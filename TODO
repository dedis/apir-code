* Optimize multi-bit scheme with selected field library
* Implement dbf-based single- and multi-bit schemes
* Implement rebalanced multi-bit scheme
* Merge single-bit and multi-bit scheme --> Kirill
* Continue with PoC app implementation (gpg, db, cli) --> Simone
* Update gRPC logic and add TLS
* General clanup and refactoring --> Kirill
* Organize iccluster servers --> Simone
* Improve config/constants
* Multi-bit scheme should verify the reconstructed entires as soon as they are computed, as before
